spring:
  datasource:
    url: jdbc:mysql://localhost:3306/health_agent?useSSL=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
  data:
    redis:
      host: localhost

# 生产环境日志级别
logging:
  level:
    root: WARN
    com.health.agent: INFO

