<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.health.agent.module.chat.mapper.ChatSessionMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.health.agent.module.chat.entity.ChatSession">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="type" property="type"/>
        <result column="status" property="status"/>
        <result column="last_message_time" property="lastMessageTime"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <result column="is_deleted" property="isDeleted"/>
    </resultMap>
    
    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, user_id, title, type, status, last_message_time, 
        created_at, updated_at, is_deleted
    </sql>
    
    <!-- 根据ID查询 -->
    <select id="findById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_session
        WHERE id = #{id} AND is_deleted = 0
    </select>
    
    <!-- 根据用户ID查询（分页） -->
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_session
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY last_message_time DESC, created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <!-- 统计用户会话数 -->
    <select id="countByUserId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM chat_session
        WHERE user_id = #{userId} AND is_deleted = 0
    </select>
    
    <!-- 插入会话 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session (
            user_id, title, type, status, last_message_time, 
            created_at, updated_at, is_deleted
        ) VALUES (
            #{userId}, #{title}, #{type}, #{status}, #{lastMessageTime},
            NOW(), NOW(), 0
        )
    </insert>
    
    <!-- 更新会话 -->
    <update id="update">
        UPDATE chat_session
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastMessageTime != null">last_message_time = #{lastMessageTime},</if>
            updated_at = NOW()
        </set>
        WHERE id = #{id} AND is_deleted = 0
    </update>
    
    <!-- 删除会话（软删除） -->
    <update id="deleteById">
        UPDATE chat_session
        SET is_deleted = 1, updated_at = NOW()
        WHERE id = #{id}
    </update>
    
</mapper>

