<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.health.agent.module.chat.mapper.ChatMessageMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.health.agent.module.chat.entity.ChatMessage">
        <id column="id" property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="role" property="role"/>
        <result column="content" property="content"/>
        <result column="tokens" property="tokens"/>
        <result column="model" property="model"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>
    
    <!-- 基础字段 -->
    <sql id="Base_Column_List">
        id, session_id, role, content, tokens, model, created_at
    </sql>
    
    <!-- 根据会话ID查询（分页） -->
    <select id="findBySessionId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
        LIMIT #{offset}, #{limit}
    </select>
    
    <!-- 统计会话消息数 -->
    <select id="countBySessionId" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM chat_message
        WHERE session_id = #{sessionId}
    </select>
    
    <!-- 插入消息 -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (
            session_id, role, content, tokens, model, created_at
        ) VALUES (
            #{sessionId}, #{role}, #{content}, #{tokens}, #{model}, NOW()
        )
    </insert>
    
    <!-- 根据会话ID删除所有消息 -->
    <delete id="deleteBySessionId">
        DELETE FROM chat_message
        WHERE session_id = #{sessionId}
    </delete>
    
    <!-- 获取最新N条消息 -->
    <select id="findLatestBySessionId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>
    
</mapper>

