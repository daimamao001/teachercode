<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.health.agent.module.kb.mapper.KnowledgeMapper">

    <resultMap id="KnowledgeResult" type="com.health.agent.module.kb.entity.Knowledge">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="category" column="category"/>
        <result property="keywords" column="keywords"/>
        <result property="embedding" column="embedding"/>
        <result property="source" column="source"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
        <result property="isDeleted" column="is_deleted"/>
    </resultMap>

    <insert id="insert" parameterType="com.health.agent.module.kb.entity.Knowledge" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO knowledge (title, content, category, keywords, embedding, source, status, created_at, updated_at, is_deleted)
        VALUES (#{title}, #{content}, #{category}, #{keywords}, #{embedding}, #{source}, #{status}, NOW(), NOW(), 0)
    </insert>

    <select id="fulltextSearch" resultMap="KnowledgeResult">
        SELECT id, title, content, category, keywords, embedding, source, status, created_at, updated_at, is_deleted
        FROM knowledge
        WHERE is_deleted = 0
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        AND MATCH(content) AGAINST (#{query} IN NATURAL LANGUAGE MODE)
        ORDER BY MATCH(content) AGAINST (#{query} IN NATURAL LANGUAGE MODE) DESC
        LIMIT #{limit}
    </select>

    <select id="likeSearch" resultMap="KnowledgeResult">
        SELECT id, title, content, category, keywords, embedding, source, status, created_at, updated_at, is_deleted
        FROM knowledge
        WHERE is_deleted = 0
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        AND content LIKE CONCAT('%', #{query}, '%')
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

</mapper>